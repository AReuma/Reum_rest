public class Explain2 {
    // 클래스 (접근 권한 제어자)
    // default : 같은 패키지에서는 접근가능
    // public : 다른 패키지에서도 접근 가능
    // protected : 같은 패키지에서는 접근 가능
    // private : 선언된 영역에서만 접근 가능. 함부로 접근 못하도록/ 직접 접근하지 못하도록.

    // getter(): public 제어자를 붙여서 다른 클래스 혹은 패키지에서도 private 변수에 접근할 수있도록 해준다. (데이터 가져오기)

    // setter(): 데이터 변경을 목적으로 한다.
}
