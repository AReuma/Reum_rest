public class Explain {

    // == : 주소 값을 비교
    // eqauls() : 값을 비교(문자열에서만)

    // 배열
    // 배열의 첫 시작 주소는 stack
    // 배열의 크기는 heap 에 만들어짐.

    // new : 기타 연산자
    // 힙 메모리에 들어가는 메모리
    // 대입연산자 앞은 저장소.

    // 원하는건 배열에 들어가 있는 값. 주소 값을 참조해야함.
    // 값에 접근하기 위해서

    // 배열
    // 같은 타입의 값들을 한번에 저장하기 위함.
    // 같은 타입의 변수들이 여러번 선언 되었을 때 배열이 생각나야 한다.

    // 배열의 선언
    // 1. 자료형[] 배열명 = new 자료형[길이]  // new를 만나면 알아서 초기화가 된다.
    // 2. 자료형 배열명[] = {값1, 값2, 값3....}
    // 3. 자료형 [] 배열명;


}
